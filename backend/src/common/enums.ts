export enum UserRole {
  SUPER_ADMIN = 'SUPER_ADMIN',
  ADMIN = 'ADMIN',
}

export enum InstanceStatus {
  STOPPED = 'STOPPED',
  RUNNING = 'RUNNING',
  RESTARTING = 'RESTARTING',
  ERROR = 'ERROR',
}
