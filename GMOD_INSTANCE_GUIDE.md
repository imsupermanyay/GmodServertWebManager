# GMOD 游戏实例管理指南

本系统使用 `ceifa/garrysmod` Docker 镜像来创建和管理 Garry's Mod 服务器实例。

## 功能说明

### 创建实例

当您创建一个新的游戏实例时，系统会：

1. **创建 Docker 容器**（使用 `ceifa/garrysmod:latest` 镜像）
2. **配置默认参数**：
   - 最大玩家数：16
   - 游戏模式：sandbox
   - 默认地图：gm_flatgrass
   - 自动重启策略：unless-stopped
3. **自动分配端口**：系统会自动为每个实例分配可用的端口
4. **保持停止状态**：新创建的实例不会自动启动，需要手动启动

### 容器配置

每个 GMOD 服务器容器使用以下默认配置：

```javascript
环境变量:
- MAXPLAYERS=16          // 最大玩家数
- GAMEMODE=sandbox       // 游戏模式
- MAP=gm_flatgrass       // 启动地图
- HOSTNAME=GMOD Server   // 服务器名称

端口映射:
- 27015/udp              // 游戏端口（UDP）
- 27015/tcp              // 游戏端口（TCP）
```

## 使用流程

### 1. 首次使用 - 拉取镜像

在创建第一个实例之前，建议先手动拉取 Docker 镜像：

```bash
docker pull ceifa/garrysmod:latest
```

这可能需要几分钟时间，取决于您的网络速度。

### 2. 创建实例

**超级管理员操作：**

1. 登录系统
2. 进入"游戏实例管理"页面
3. 点击"创建实例"按钮
4. 填写实例信息：
   - **实例名称**：必填，不能重复
   - **宿主机目录**：可选，用于挂载插件、地图等文件
   - **容器目录**：可选，通常为 `/home/steam/gmodserver/garrysmod`
5. 点击确定

系统会自动：
- 创建 Docker 容器
- 分配可用端口
- 保存实例信息到数据库

### 3. 分配管理员

**超级管理员操作：**

1. 在"游戏实例管理"页面，点击实例的"编辑"按钮
2. 在"分配管理员"下拉菜单中选择普通管理员
3. 点击保存

### 4. 管理实例

**普通管理员操作：**

1. 登录系统
2. 进入"我的实例"页面
3. 可以看到分配给自己的所有实例
4. 对实例进行操作：
   - **开机**：启动服务器
   - **关机**：停止服务器
   - **重启**：重启服务器
   - **查看控制台**：查看服务器日志输出

### 5. 查看实例信息

通过 API 可以获取实例的详细信息：

```
GET /api/instances/:id/info
```

返回信息包括：
- 实例基本信息
- 容器状态
- 分配的端口号
- 创建时间

## 挂载目录说明

如果您需要为服务器添加自定义内容（插件、地图、模型等），可以在创建实例时配置挂载目录：

### 推荐挂载结构

```
宿主机目录: /data/gmod/server1
容器目录: /home/steam/gmodserver/garrysmod
```

然后在宿主机目录中放置文件：

```
/data/gmod/server1/
├── addons/           # 插件目录
│   ├── darkrp/
│   └── ...
├── gamemodes/        # 游戏模式
├── maps/            # 地图文件
└── lua/             # Lua 脚本
```

## 端口管理

系统使用动态端口分配策略：

- 每个实例会自动分配一个可用的端口
- 端口映射：容器内 27015 → 宿主机随机端口
- 可以通过容器信息 API 查询实际分配的端口

## 常见问题

### Q: 创建实例需要多长时间？

A: 通常只需要几秒钟。如果是第一次创建，需要先下载 Docker 镜像，可能需要 5-10 分钟。

### Q: 实例创建后为什么不能立即连接？

A: 新创建的实例默认是停止状态，需要先点击"开机"按钮启动服务器。

### Q: 如何知道服务器的连接地址？

A: 通过实例信息 API 可以查看分配的端口，连接地址为：`服务器IP:分配的端口`

### Q: 可以修改服务器配置吗？

A: 目前版本支持基本配置。高级配置可以通过挂载目录，修改配置文件实现。

### Q: 实例删除后数据会保留吗？

A: 不会。删除实例会同时删除 Docker 容器和所有相关数据。如果有重要数据，请先备份。

### Q: 如何查看服务器是否正常运行？

A: 可以通过以下方式：
1. 查看实例状态（运行中/已停止）
2. 点击"查看控制台"查看服务器日志
3. 尝试连接到服务器

## Docker 镜像说明

本系统使用 `ceifa/garrysmod` 镜像，特点：

- 基于 Linux 系统
- 自动安装和更新 GMOD 服务器
- 支持通过环境变量配置
- 轻量级，占用资源少

更多信息请访问：https://hub.docker.com/r/ceifa/garrysmod

## API 接口

### 创建实例
```
POST /api/instances
{
  "name": "server-1",
  "hostDirectory": "/data/gmod/server1",
  "containerDirectory": "/home/steam/gmodserver/garrysmod"
}
```

### 获取实例信息
```
GET /api/instances/:id/info
```

### 启动实例
```
POST /api/instances/:id/start
```

### 停止实例
```
POST /api/instances/:id/stop
```

### 重启实例
```
POST /api/instances/:id/restart
```

### 查看日志
```
GET /api/instances/:id/logs
```

## 最佳实践

1. **命名规范**：使用有意义的实例名称，如 `rp-server-1`, `sandbox-test` 等
2. **挂载目录**：为每个实例使用独立的挂载目录
3. **定期备份**：重要数据定期备份到其他位置
4. **监控日志**：定期查看控制台日志，及时发现问题
5. **资源管理**：根据服务器负载合理分配实例数量
